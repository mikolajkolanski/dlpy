# DlPy

Simple gradient descent project in pure numpy. 

It implements **SGD optimizer**, **LeakyReLU** and **Linear** layers that can be combined into multi-layer MLP.

It achives 94.8% accuracy on MNIST dataset.